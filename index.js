/*
JS jargon;
ASI: automatic semi-colon insertion:

Declirations in js:
no longer in use; Var: block-level scoping; inside the whole function
Let only available within the block {}; can be reassigned not redeclared;
Const: block level scoping cannot be changed? cannot be reassigned or redeclared within the same blcok; xss?

composite datatypes:can be changed during execution
array: nig = [3,3,3,2,3];?call
Object: siva {age: "", fjdk:""}
Operators:
  Arethmatic:Math:
  Comparison: compair and return True or false;
  != inequal; == equal, ===equal with the same datatype;
  logical: man the return of cpmparison: &&: ||: !x;
call value form a var ${}





get function call/ get interactivity/ get event listeners/ get api premissions/ get xss/ get php/ get sql/ upload/ man npm; lcommit in npm;obsolete js; obfuscated js; deobfuscate k;?2 values for onclick
?inetsynced animations;
?hidden items: animation> glep tv, dim bg 1sec during; Onclick: A:glep.LRR, B:glep.RTL; C:khaby.LTR&RTL;
dim the button: spam prevention; click conunt and act accordingly
N8N CI/CD:
A:fetch gh commits: linkedin & portfolio 
B:fetch linkedin posts for a webpage

JS usie cases; purpose; interativity

js basic syntax;
built-InFunctions("");
js built in functions:
  pop-Ups in js:
  alert(""); message;
  confirm("message"); returns a boolina y/n;
  Prombt("message"); user input prombt;



DOM Man:
document.getELementById("live").innerHtml = "siva"; changes the id values to siva;
  get the css;


DOM man;
Handeling events
from validation;
feedback; aka responsive forms
reaload the feed
visual effects


SDLC IN js:
 use gest and mocha to make the tests
 async: await; run while
 functions for predicatble code
es6 the js syntax; constantly updatedj;
try catfch block for error handeling; chrome dev tools; console;
js for front end;
  Reach
  Vue
  Angular

  Express for backend
  runtime environment:
  Node.js

Mocha or Jasmine for testing before the code is even written

Security: 
xss; csrm;

Maintainance; documentation;
JSDOC; doc




??:
?add capti
?multiple versions for page for diffferent devices;  ?view port for mobile;sizes relivant to device width and height;?size percentage?
?emojies
Object.addEventListener ("", function)
*/

/*
function dilemmas(fever,simaska)
{if (fever =>) :
addEventListener
}

let makemecv = pdfmaker()
import lablemake from "lablemake";
let pdfmaker =
  document.getElementByID.(live)
*/

function changer() {
  document.getElementById('dates').style.color = "red";
}


/*looping: do {} while () exec once and then checks the con: if t exec again;  */

 function sarika()
  {
    for ( let si = 33; si > 45; si++)
    {
    console.log(si);
   /* 
while (si == 44)
{
  console.log("ok")
}
*/
 }
}


